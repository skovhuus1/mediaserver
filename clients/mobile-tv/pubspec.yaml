name: boltbytes_media
description: BoltBytes Media native client for Android phones, tablets and TV.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: 10.0.0
  firebase_core: 4.13.0
  firebase_messaging: 16.5.0
  http: ^1.6.0
  shared_preferences: ^2.5.5
  url_launcher: ^6.3.2
  video_player: ^2.14.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
