export const receiverHtml = `<!doctype html>
<html lang="da"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>BoltBytes Cast</title>
<style>
:root{color-scheme:dark;font-family:Arial,sans-serif}*{box-sizing:border-box}html,body,cast-media-player{width:100%;height:100%;margin:0;overflow:hidden;background:#050706}body{position:relative;color:#f6f4ee}cast-media-player{position:absolute;inset:0;--theme-hue:38;--progress-color:#efb75e;--splash-color:#070a09}.ambient{position:absolute;inset:-4%;z-index:1;background:radial-gradient(circle at 76% 18%,#d99a3938,transparent 35%),linear-gradient(125deg,#09100fdd,#040606f2);background-position:center;background-size:cover;filter:blur(24px) saturate(.85);opacity:.55;transform:scale(1.08);transition:opacity .7s}.shade{position:absolute;inset:0;z-index:2;background:linear-gradient(180deg,#050706bb 0,transparent 24%,transparent 58%,#050706e8 100%);pointer-events:none}.chrome{position:absolute;inset:0;z-index:3;display:flex;padding:5.5vh 6vw;flex-direction:column;justify-content:space-between;pointer-events:none;transition:opacity .6s}.top{display:flex;align-items:center;justify-content:space-between}.brand{display:flex;align-items:center;gap:16px}.mark{display:grid;width:58px;height:58px;place-items:center;border:1px solid #efb75e55;border-radius:17px;background:linear-gradient(145deg,#efbc68,#bb7927);box-shadow:0 20px 55px #0008}.mark i{position:relative;width:25px;height:31px;border:4px solid #1d150b;border-radius:6px}.mark i:after{content:"";position:absolute;right:-9px;top:8px;width:13px;height:11px;border:4px solid #1d150b;border-left:0;border-radius:0 7px 7px 0}.brand strong{display:block;font-size:25px;letter-spacing:-.04em}.brand small{display:block;margin-top:3px;color:#aaa99f;font-size:9px;font-weight:800;letter-spacing:.25em}.status{display:flex;align-items:center;gap:9px;padding:10px 15px;border:1px solid #ffffff18;border-radius:999px;background:#080c0bd9;box-shadow:0 12px 40px #0007;font-size:12px;font-weight:800}.status i{width:8px;height:8px;border-radius:50%;background:#efb75e;box-shadow:0 0 0 5px #efb75e1a}.idle{position:absolute;top:50%;left:50%;z-index:4;width:min(70vw,800px);text-align:center;transform:translate(-50%,-50%);transition:opacity .45s,transform .45s}.idle .glyph{display:grid;width:94px;height:94px;margin:0 auto 25px;place-items:center;border:1px solid #ffffff16;border-radius:28px;background:#101615d9;box-shadow:0 35px 100px #000a}.idle .glyph:before{content:"";width:34px;height:34px;border:4px solid #efb75e;border-top-color:transparent;border-radius:50%;animation:spin 1.05s linear infinite}.idle h1{margin:0;font-size:clamp(38px,5vw,72px);letter-spacing:-.055em}.idle p{margin:13px 0 0;color:#aaafac;font-size:17px}.now{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:16px 30px;align-items:end}.copy span{display:block;margin-bottom:8px;color:#efbd74;font-size:11px;font-weight:900;letter-spacing:.18em}.copy h1{max-width:70vw;margin:0;overflow:hidden;font-size:clamp(31px,4.2vw,65px);letter-spacing:-.055em;text-overflow:ellipsis;white-space:nowrap}.copy p{margin:7px 0 0;color:#b4bab5;font-size:16px}.facts{display:flex;gap:8px;align-items:center;justify-content:flex-end;flex-wrap:wrap}.facts span{padding:8px 11px;border:1px solid #ffffff14;border-radius:8px;background:#0b100fe8;color:#c8cec9;font-size:11px;font-weight:750}.progress{grid-column:1/-1}.bar{height:4px;overflow:hidden;border-radius:5px;background:#ffffff1c}.bar i{display:block;width:0;height:100%;border-radius:5px;background:#efb75e;transition:width 1s linear}.time{display:flex;justify-content:space-between;margin-top:7px;color:#8d9691;font-size:10px}body[data-state="idle"] .idle .glyph:before{animation:none;border:0;border-radius:8px;background:linear-gradient(135deg,#efb75e,#b97626);clip-path:polygon(0 0,100% 50%,0 100%)}body[data-state="playing"] .idle,body[data-state="paused"] .idle{opacity:0;transform:translate(-50%,-47%)}body[data-state="playing"] .chrome:not(.fresh){opacity:.12}body[data-state="playing"] .chrome:not(.fresh) .now{opacity:0}body[data-state="error"] .idle .glyph:before{animation:none;border:0;border-radius:2px;background:#ef795f;clip-path:polygon(42% 0,58% 0,58% 67%,42% 67%,42% 0,42% 78%,58% 78%,58% 100%,42% 100%)}body[data-state="error"] .status i{background:#ef795f}body[data-state="buffering"] .status i{animation:pulse 1s ease-in-out infinite}@keyframes spin{to{transform:rotate(360deg)}}@keyframes pulse{50%{opacity:.35;transform:scale(.75)}}
</style>
<script src="https://www.gstatic.com/cast/sdk/libs/caf_receiver/v3/cast_receiver_framework.js"></script></head>
<body data-state="idle"><cast-media-player></cast-media-player><div class="ambient" id="ambient"></div><div class="shade"></div><div class="chrome" id="chrome"><div class="top"><div class="brand"><span class="mark"><i></i></span><span><strong>BoltBytes</strong><small>MEDIA SERVER · CAST</small></span></div><span class="status"><i></i><b id="status">Klar til Cast</b></span></div><div class="now"><div class="copy"><span>AFSPILLER FRA DIN SERVER</span><h1 id="title">BoltBytes Media</h1><p id="subtitle">Vælg en titel på din enhed</p></div><div class="facts"><span id="method">Klar</span><span id="quality">Automatisk kvalitet</span><span id="track">Ingen undertekster</span></div><div class="progress"><div class="bar"><i id="bar"></i></div><div class="time"><span id="elapsed">0:00</span><span id="duration">0:00</span></div></div></div></div><section class="idle"><div class="glyph"></div><h1 id="idleTitle">Klar til Cast</h1><p id="idleCopy">Vælg en film eller serie i BoltBytes</p></section>
<script>(()=>{
const context=cast.framework.CastReceiverContext.getInstance();const player=context.getPlayerManager();const textTracks=player.getTextTracksManager();const el=(id)=>document.getElementById(id);let telemetry=null;let heartbeatTimer=null;let clockTimer=null;let chromeTimer=null;let previousState='starting';let stallCount=0;let released=false;
const cleanUrl=(value)=>String(value||'').replace(/["\\\n\r]/g,'');const formatTime=(seconds)=>{const value=Math.max(0,Math.floor(Number(seconds)||0));const h=Math.floor(value/3600);const m=Math.floor(value%3600/60);const s=value%60;return h?String(h)+':'+String(m).padStart(2,'0')+':'+String(s).padStart(2,'0'):String(m)+':'+String(s).padStart(2,'0')};
const runtimeState=()=>{const value=String(player.getPlayerState()||'').toLowerCase();if(value.includes('buffer'))return'buffering';if(value.includes('pause'))return'paused';if(value.includes('play'))return'playing';return'starting'};const activeTrack=()=>{try{const track=textTracks.getActiveTracks()[0];return track?(track.name||track.language||'Undertekster'):'Ingen undertekster'}catch(_){return telemetry&&telemetry.subtitleTrack||'Ingen undertekster'}};
const quality=()=>{const height=Number(telemetry&&telemetry.currentHeight);const bitrate=Number(telemetry&&telemetry.currentBitrate);const resolution=height>=2160?'4K':height>0?String(height)+'p':'Auto';const rate=bitrate>0?(bitrate/1000000).toFixed(bitrate>=10000000?0:1)+' Mbps':'';return rate?resolution+' · '+rate:resolution};
const showChrome=()=>{el('chrome').classList.add('fresh');clearTimeout(chromeTimer);chromeTimer=setTimeout(()=>el('chrome').classList.remove('fresh'),8000)};const setState=(state,label,headline,copy)=>{document.body.dataset.state=state;el('status').textContent=label;if(headline)el('idleTitle').textContent=headline;if(copy)el('idleCopy').textContent=copy;if(state!=='playing')showChrome()};
const updateClock=()=>{const current=Math.max(0,Number(player.getCurrentTimeSec()||0));const media=player.getMediaInformation();const duration=Number(media&&media.duration)||Math.max(0,Number(telemetry&&telemetry.fullDurationMs||0)/1000-Number(telemetry&&telemetry.timelineOffsetMs||0)/1000);el('elapsed').textContent=formatTime(current);el('duration').textContent=formatTime(duration);el('bar').style.width=duration>0?String(Math.min(100,current/duration*100))+'%':'0%';el('quality').textContent=quality();el('track').textContent=activeTrack()};
const heartbeat=async()=>{if(!telemetry||!telemetry.heartbeatUrl||released)return;const state=runtimeState();if(state==='buffering'&&previousState!=='buffering')stallCount+=1;previousState=state;const media=player.getMediaInformation();const mediaDuration=Number(media&&media.duration);const localPosition=Math.max(0,Math.round(Number(player.getCurrentTimeSec()||0)*1000));const duration=Number(telemetry.fullDurationMs);try{await fetch(telemetry.heartbeatUrl,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({runtimeState:state,positionMs:Math.max(0,Number(telemetry.timelineOffsetMs||0)+localPosition),durationMs:Number.isFinite(duration)&&duration>0?Math.round(duration):Number.isFinite(mediaDuration)?Math.max(0,Math.round(mediaDuration*1000)):undefined,currentBitrate:Number(telemetry.currentBitrate)||null,currentHeight:Number(telemetry.currentHeight)||null,bufferAheadMs:null,stallCount:stallCount,playbackRate:1,subtitleTrack:activeTrack()==='Ingen undertekster'?null:activeTrack()}),keepalive:true})}catch(_){}};
const release=async(reason)=>{if(released||!telemetry||!telemetry.releaseUrl)return;released=true;clearInterval(heartbeatTimer);clearInterval(clockTimer);await fetch(telemetry.releaseUrl,{method:'DELETE',headers:{'content-type':'application/json'},body:JSON.stringify({reason:reason}),keepalive:true}).catch(()=>{})};
const loadUi=(request)=>{const media=request&&request.media||{};const metadata=media.metadata||{};telemetry=media.customData||{};released=false;previousState='starting';stallCount=0;const title=telemetry.title||metadata.title||'Ukendt titel';const subtitle=telemetry.subtitle||metadata.subtitle||'Afspiller fra BoltBytes';const poster=telemetry.posterUrl||(metadata.images&&metadata.images[0]&&metadata.images[0].url);el('title').textContent=title;el('subtitle').textContent=subtitle;el('method').textContent=telemetry.methodLabel||'Cast';el('quality').textContent=quality();el('track').textContent=telemetry.subtitleTrack||'Ingen undertekster';el('ambient').style.backgroundImage=poster?'linear-gradient(120deg,#09100fd9,#040606f0),url("'+cleanUrl(poster)+'")':'';setState('loading','Forbereder','Forbereder stream',title);clearInterval(heartbeatTimer);clearInterval(clockTimer);heartbeatTimer=setInterval(heartbeat,5000);clockTimer=setInterval(updateClock,1000);heartbeat();updateClock();return request};
player.setMessageInterceptor(cast.framework.messages.MessageType.LOAD,loadUi);player.setSupportedMediaCommands(cast.framework.messages.Command.SEEK|cast.framework.messages.Command.PAUSE);
player.addEventListener(cast.framework.events.EventType.PLAYER_LOAD_COMPLETE,()=>{setState('playing','Afspiller');showChrome();heartbeat()});player.addEventListener(cast.framework.events.EventType.PLAYING,()=>{setState('playing','Afspiller');heartbeat()});player.addEventListener(cast.framework.events.EventType.PAUSE,()=>{setState('paused','Sat på pause');heartbeat()});
player.addEventListener(cast.framework.events.EventType.BUFFERING,(event)=>{if(event.isBuffering){if(previousState!=='buffering')stallCount+=1;previousState='buffering';setState('buffering','Buffering','Henter mere video','Afspilningen fortsætter automatisk')}else setState(runtimeState(),'Afspiller');heartbeat()});player.addEventListener(cast.framework.events.EventType.BITRATE_CHANGED,(event)=>{if(telemetry&&Number(event.totalBitrate)>0)telemetry.currentBitrate=Math.round(Number(event.totalBitrate));updateClock();heartbeat()});player.addEventListener(cast.framework.events.EventType.TRACKS_CHANGED,()=>{updateClock();heartbeat()});player.addEventListener(cast.framework.events.EventType.SEEKING,()=>setState('buffering','Søger','Finder positionen','Et øjeblik...'));player.addEventListener(cast.framework.events.EventType.SEEKED,()=>{setState(runtimeState(),'Afspiller');heartbeat()});player.addEventListener(cast.framework.events.EventType.ERROR,(event)=>{setState('error','Afspilningsfejl','Streamen kunne ikke afspilles',String(event.reason||'Prøv igen fra din enhed'));heartbeat()});player.addEventListener(cast.framework.events.EventType.MEDIA_FINISHED,()=>{setState('idle','Afsluttet','Afspilning afsluttet','Vælg den næste titel på din enhed');release('media_finished')});player.addEventListener(cast.framework.events.EventType.REQUEST_STOP,()=>release('receiver_stopped'));window.addEventListener('error',(event)=>setState('error','Receiver-fejl','BoltBytes receiver stoppede',event.message||'Genstart Cast-sessionen'));
const playbackConfig=new cast.framework.PlaybackConfig();playbackConfig.autoResumeNumberOfSegments=1;context.start({playbackConfig:playbackConfig});
})()</script></body></html>`;
